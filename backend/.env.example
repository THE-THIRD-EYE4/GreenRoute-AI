# Optional. Enables AI-written explanations.
# The platform runs fully without this: with no key set, explanations fall back
# to deterministic templates built from real system numbers.
# NEVER commit a real key.
GEMINI_API_KEY=
